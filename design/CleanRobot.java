package design;


/*
Map<String, boolean[]> --> String represent coordinates, boolean[4] represents the directions 
we've explored

dfs(i, j, map, Stack<Path>, int dir){
    if(!map.contains(getString(i,j))
 	map.put(string, new boolean[4]);
    for(i=0;i<4;i++){
    	if(!map.get(String)[i]){
    	    while(canMove)
    	    dfs()
    	    map.get(String)[i] = true; 
    	}
    }
}

*/
public class CleanRobot {

}
