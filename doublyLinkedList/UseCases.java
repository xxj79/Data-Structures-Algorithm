package doublyLinkedList;


/*
The reason we may need a doublylinkedlist:

Reverse look up

O(1) delete(node) operation, while using LinkedList, we 
have to traverse from the beginning to find predecessor
which is O(n) operation



*/
public class UseCases {

}
