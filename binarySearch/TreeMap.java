package binarySearch;



/*
The map is sorted according to the natural ordering of its key

ceilingKey(K key): 
  return the least key >= the given key, or null if
there is no such key.

floorKey(K key): 
  return the greatest key <= the given key, or null if 
there is no such key

higherKey(K key): >
lowerKey(K key): <

firstKey(K key): 
  return the first key (lowerst) key currently in this map
lastKey(K key): 
  return the last key (highest) key currently in the map

tailMap(K fromKey): 
  return a view of the portion of this map whose keys 
are greater than or equal to framKey.
 
*/
public class TreeMap {

}
